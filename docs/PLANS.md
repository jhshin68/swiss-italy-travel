# PLANS.md — 마일스톤·로드맵

> 10월 여행 전 완성이 목표. 역산해서 마일스톤을 잡는다.

## 타임라인 총괄

```
2026년 3월 말   ← 지금 (설계 문서 완성)
     ↓
4월~5월         Phase 1: 기반 구축 + 핵심 기능
     ↓
6월~7월         Phase 2: P0 완성 + P1 착수
     ↓
8월             Phase 3: 통합 테스트 + 데이터 시딩
     ↓
9월             Phase 4: 가족 베타 테스트 + 버그 수정
     ↓
10월 초         🚀 여행 출발 (실전 사용)
     ↓
10월 말~        Phase 5: 회고 + 상용화 검토
```

## Phase 1: 기반 구축 (4월~5월)

### 마일스톤 1.1 — 프로젝트 셋업 (1주)
```
✅ Next.js 16.2.1 + TypeScript 프로젝트 초기화
✅ Tailwind CSS + shadcn/ui 설정
✅ ESLint + Prettier 설정
✅ GitHub 레포 생성 + 브랜치 전략 (main/dev)
✅ Vercel 연결 + 자동 배포
✅ PWA 기본 설정 (next-pwa)
✅ 환경변수 구조 (.env.example)
```

### 마일스톤 1.2 — 백엔드 인프라 (1주)
```
✅ Oracle VM에 Express + SQLite(better-sqlite3) 서버 셋업
✅ Caddy Docker 컨테이너로 리버스 프록시 설정 (HTTPS, Let's Encrypt)
   - slp-travel.duckdns.org → Express API (포트 3001)
   - slpower-n8n.duckdns.org → n8n (포트 5678)
✅ SQLite 스키마 작성 (backend/src/db/schema.sql — 테이블 11개)
✅ 초기 마이그레이션 스크립트 (backend/src/db/migrate.ts)
✅ DB 연결 모듈 (backend/src/db/connection.ts)
✅ API 헬스체크 엔드포인트 (GET /api/health)
✅ Express 서버 진입점 + CORS + Helmet + Morgan (backend/src/index.ts)
✅ 글로벌 에러 핸들러 (backend/src/middleware/errorHandler.ts)
□ GitHub Actions CI/CD 파이프라인 (.github/workflows/ci.yml 초안 있음, 검증 필요)
```

### 마일스톤 1.3 — 인증 + 앱 셸 (1주)
```
□ PIN 로그인 API + 프론트 (가족 공통 PIN 1개 + 멤버 선택)
□ 멤버 선택 화면
□ JWT 발급·검증 (30일 만료, refresh 없음)
□ 하단 탭 바 구현
□ 상단 헤더 (D-Day, 도시명)
□ 레이아웃 시스템 (모바일 퍼스트)
□ 오프라인 배너 컴포넌트
```

### 마일스톤 1.4 — 데이터 시딩 (1주)
```
□ itinerary.ts → DayPlan + Spot 변환 시딩 스크립트 (108개 장소)
□ InfoCard 초기 데이터 작성 (숙소·긴급연락처·교통 팁)
□ 긴급 연락처 시딩 데이터 (스위스/이탈리아 대사관, 보험사 등)
□ 멤버 4인 초기 데이터 (진형/지현/동우/유진)
□ npm run seed 명령어 (backend/src/db/seed.ts)
□ 시딩 후 검증 쿼리 (장소 108개, 멤버 4명 확인)
```

## Phase 2: P0 기능 구현 (6월~7월)

### 마일스톤 2.1 — 일정 기능 (2주)
```
□ 일정 메인 페이지 (타임라인 뷰)
□ 날짜 네비게이션 (스와이프 + 날짜 바)
□ 장소 카드 (상세 확장)
□ 현재 진행 중 일정 강조
□ 날씨 대안 표시
□ 일정 추가/수정/삭제 모달
□ 예약 번호 클립보드 복사
□ API: GET/POST/PUT/DELETE spots
```

### 마일스톤 2.2 — 경비 기능 (2주)
```
□ 경비 메인 (오늘/전체/정산 서브 탭)
□ 지출 추가 모달
□ 통화 선택 + KRW 환산
□ 카테고리 선택 (6종)
□ 결제자 + 분할 방식
□ 카테고리별/일별/결제자별 통계
□ 정산 알고리즘 (최소 거래 수)
□ 정산 결과 화면
□ API: expenses CRUD + settlement
```

### 마일스톤 2.3 — 비상 정보 + 대시보드 (1주)
```
□ 정보 탭 메인 (카테고리별 카드)
□ 긴급 연락처 (고정 표시)
□ 오늘 숙소 자동 표시
□ 전화 바로 걸기 (tel: 링크)
□ 홈 대시보드 (오늘 요약)
□ 오늘 일정 카드
□ 오늘 지출 요약 카드
□ 비상 정보 요약 카드
```

### 마일스톤 2.4 — 오프라인 (1주)
```
□ Service Worker 캐싱 전략 구현
□ IndexedDB 로컬 저장 (경비)
□ 온/오프라인 감지 훅
□ 오프라인 → 온라인 동기화
□ 오프라인 배너 UX
□ 캐시 무효화 전략
```

## Phase 3: 통합·안정화 (8월)

### 마일스톤 3.1 — 통합 테스트 (2주)
```
□ E2E 테스트 시나리오 작성 (핵심 플로우 5개)
□ 오프라인 시나리오 테스트
□ 모바일 브라우저 호환 테스트 (iOS Safari, Android Chrome)
□ PWA 설치 테스트 (양 플랫폼)
□ 성능 최적화 (Lighthouse 점수 90+)
□ 최종 데이터 시딩 (확정 일정 반영)
```

### 마일스톤 3.2 — P1 기능 (선별) (2주)
```
□ 지도 페이지 (Leaflet + 오늘 장소 표시)
□ 환율 자동 갱신 (n8n 워크플로)
□ 날씨 표시 (캐싱)
□ (선택) 실시간 위치 공유
□ (선택) 오프라인 지도 타일
```

## Phase 4: 베타 테스트 (9월)

```
□ 가족 4인 디바이스에 PWA 설치
□ 1주일 일상에서 사용 (경비 기록 연습)
□ 피드백 수집 + 버그 수정
□ 최종 일정·예약 데이터 업데이트
□ 오프라인 시나리오 실제 테스트 (비행기 모드)
□ 여행 출발 전 최종 점검
```

## Phase 5: 회고 + 상용화 검토 (10월 말~)

```
□ 여행 중 사용 경험 회고
□ 가족 피드백 정리
□ 버그/개선 사항 목록화
□ 상용화 여부 결정
□ (상용화 시) 다중 여행, OAuth, 결제 등 P2 기능 로드맵
```

## 주간 작업 흐름

```
월요일: 주간 목표 설정 (exec-plans/active/에 작성)
화~금:  에이전트 구현 → 오너 리뷰 → 피드백 반영
금요일: 주간 회고 (완료 → exec-plans/completed/ 이동)
       tech-debt-tracker.md 업데이트
```

## 리스크 관리

| 리스크 | 확률 | 영향 | 대응 |
|--------|------|------|------|
| 일정 지연 (에이전트 한계) | 중 | 높 | P1 기능 과감히 제거, P0 집중 |
| Oracle VM 리소스 부족 | 낮 | 중 | DB + API가 가벼워서 무료 티어 충분 |
| Safari PWA 제한 | 중 | 중 | Background Sync fallback 구현 |
| 여행 일정 변경 | 높 | 낮 | JSON 시딩 스크립트로 빠르게 재반영 |
| 오프라인 동기화 버그 | 중 | 높 | 8월에 집중 테스트, 단순 LWW 전략 유지 |

---

> **버전:** v0.2
> **최종 수정:** 2026-03-29
